## Kugo Music Converter v0.5.1

Kugo Music Converter 是一款酷狗音乐加密文件解密转换工具，支持 KGG / KGM / KGMA / VPR / NCM 等格式的解密与转码

### 使用方法

1. 下载并解压发布包
2. 双击 `start.hta` 启动（备选：`start.bat`）
3. 浏览器自动打开操作界面，拖入文件或选择文件夹即可开始转换

### 系统要求

- Windows 10 / 11 (64-bit)
- 无需安装任何运行时环境

### 更新内容

**大文件转换（#4、#6）**
- 默认单文件上限从 80 MiB 提升到 1 GiB，解决较大的 KGM / KGMA / KGG / VPR / NCM 文件在加入队列时被跳过的问题
- 前端与后端统一使用相同的单文件限制，并通过 `/api/config` 同步到界面
- 文件过大时显示实际大小与当前上限，便于判断是否需要调整配置
- 浏览器单次上传总计上限为 2028 MiB，超过时可分批处理或使用目录扫描

**队列与错误提示**
- 修复扫描结果加入转换队列时重复计算新增数量，导致队列无法填满剩余名额的问题
- 拖放和文件选择日志现在会准确显示已加入、格式不支持、文件过大、重复及超过数量上限的数量
- 请求大小超限时后端返回 HTTP 413，并给出分批处理或使用目录扫描的建议

**配置与发布流程**
- 配置示例与实际默认值保持一致：1 GiB 单文件、500 个文件、发布包内 `tools/ffmpeg.exe`
- 发布包新增 `VERSION` 文件，启动器与后端显示的版本保持一致
- 新增前端队列回归测试和 Windows 发布打包脚本

**测试与质量**
- Go 全量测试及 `go vet` 通过
- 前端队列回归测试及全部 JavaScript 文件语法检查通过
- Windows amd64 后端构建、`--version` 校验及发布包内容检查通过
