## Kugo Music Converter v0.6.0

Kugo Music Converter 是一款本机加密音频解密转换工具，支持酷狗、网易云、酷我和传统 QQ 音乐格式转换为 MP3、FLAC、WAV 或原始音频格式。

v0.6.0 是一次重大桌面化升级：程序由“HTA/BAT 启动器 + 本地服务 + 浏览器页面”升级为单个 Windows 桌面 EXE。

![Kugo Music Converter v0.6.0 界面](https://raw.githubusercontent.com/skxxxkx666/Kugo-Music-Converter/v0.6.0/assets/screenshot.png)

**亮点速览**：双击即用无需浏览器 · FFmpeg 内嵌免安装 · 新增酷我 KWM 与传统 QMC 解密 · 自动发现本机音乐 · 任务栏进度与完成通知 · 全屏自适应界面

### 使用方法

#### Windows（不支持 Windows 7）

| 你的情况 | 下载这个 |
|---|---|
| 大多数用户（推荐） | `Kugo-Music-Converter-v0.6.0-windows-amd64-setup.exe`（安装器，不需要管理员权限） |
| 不想安装、直接运行 | `Kugo-Music-Converter-v0.6.0-windows-amd64.exe`（便携版） |
| 受管控环境 / 无法安装 WebView2 | `Kugo-Music-Converter-v0.6.0-windows-amd64-webview2-setup.exe` 或 `-webview2.exe`（内置运行时，构建约 339 MiB，首次启动后缓存约 661 MiB） |

1. 下载适合的 EXE
2. 双击 EXE 启动
3. 拖入文件或文件夹，也可以使用“选择音频文件”、“添加文件夹”或“查找本机音乐”
4. 选择输出格式、音质、并发数和输出目录
5. 点击“开始转换”

首次启动会准备内嵌 FFmpeg，后续启动会直接复用已校验的本机缓存。内置 WebView2 版还会在首次启动时校验并解压 Fixed Runtime，因此第一次打开更慢且会占用额外磁盘空间。

### 系统要求

- Windows 10 / 11（64 位）
- 不支持 Windows 7
- 标准版需要 Microsoft Edge WebView2 Runtime；Windows 10/11 与新版 Edge 通常已包含
- 内置 WebView2 版无需安装系统 WebView2 Runtime

### 本次重大更新

**单 EXE 桌面应用**

- 使用 Wails v2 构建完整 Windows 桌面窗口
- 不再需要打开 `start.hta` 或 `start.bat`
- 不再跳转到浏览器
- 不再依赖 `localhost:8080`
- 转换进程不监听本地 TCP 端口

**内嵌 FFmpeg 运行时**

- FFmpeg 压缩后嵌入桌面 EXE
- 首次启动自动解压到 `%LOCALAPPDATA%`
- 解压时校验 SHA-256，防止损坏文件被启用
- 后续启动验证并复用缓存
- 用户无需单独准备 `tools/ffmpeg.exe`

**更多加密音乐格式**

- 新增酷我 `.kwm`
- 新增传统 QQ 音乐 `.qmc0`、`.qmc2`、`.qmc3`、`.qmc4`、`.qmc6`、`.qmc8`
- 新增 `.qmcflac`、`.qmcogg` 和伴奏 `.tkm`
- 新版 `.mflac` / `.mgg` 需要额外设备密钥，本版本不宣称支持

**文件选择与队列**

- 支持原生多文件选择
- 支持原生目录选择和递归扫描
- 支持把文件或文件夹拖入窗口
- 支持查找本机音乐：读取酷狗 `KuGou.ini` 和网易云、酷我、QQ 音乐受限客户端配置中的自定义下载路径，并在固定磁盘根目录检查常见文件夹；结果按软件分组显示真实品牌图标，支持分组全选、整行点选和折叠，由用户勾选后加入队列；不遍历整块磁盘，也不修改源文件
- 自动过滤不支持的格式并去重
- 队列支持单项移除和全部清空
- 队列行显示彩色扩展名徽章，支持单击选中、双击试听、右键上下文菜单（试听 / 定位 / 重试 / 复制文件名 / 复制路径 / 移除）
- 键盘快捷键：Ctrl+O 选择文件、Enter 开始转换、Esc 取消转换、Delete 移除选中项
- 显示文件大小、转换状态和错误详情

**转换设置**

- 支持 MP3、FLAC、WAV 和保持原格式
- 支持 MP3 音质选择
- 支持并发数选择，并根据 CPU 动态限制上限
- 支持选择和直接打开输出目录
- 自动保存格式、音质、并发数、输出目录和数据库路径

**KGG 数据库**

- 自动检测 `KGMusicV3.db`
- 支持手动选择
- 支持一键重新检测
- 保存上次选择的有效路径
- 继续使用内存解库、完整性校验和进程缓存

**进度与任务控制**

- 使用桌面事件实时显示转换进度
- 显示当前文件、处理阶段、总百分比和 ETA
- 进度实时同步到 Windows 任务栏图标，失败时显示错误状态
- 转换结束且窗口不在前台时，任务栏闪烁并发送 Windows 通知
- 支持中途取消
- 取消前提供原生确认窗口
- 取消不会删除已经成功生成的文件
- 转换进行中关闭窗口时会弹出退出确认

**结果与失败恢复**

- 成功结果可直接试听
- 可在资源管理器中定位输出文件
- 可打开输出目录
- 失败项显示错误代码、说明、建议和详情
- 支持单项重试和全部失败项重试
- 支持导出 UTF-8 CSV 失败列表

**历史与诊断**

- 最多保存 50 次本机转换历史
- 支持恢复设置、删除单条和清空全部
- 修复 Windows 上确认清空或删除历史后操作没有生效的问题，确认框默认选择“否”
- 高级工具中显示本次运行诊断记录
- 支持导出桌面诊断 LOG

**高级目录扫描**

- 支持一次扫描多个目录
- 支持递归、非递归和自定义扩展名
- 支持复制文件名、复制完整路径和导出 CSV
- 支持把扫描到的加密音频加入转换队列

**更新检查**

- 显示最新版本、发布日期和更新摘要
- 支持忽略指定版本
- 支持下载并安装对应架构和变体的官方安装器；安装器必须配有 SHA-256 文件且校验通过
- 自动更新前会弹出原生确认；仍可打开本项目 GitHub Releases 手动更新
- 转换本身仍然完全离线；只有检查更新会访问网络

**运行时缓存管理**

- 高级工具中可统计应用自身管理的 FFmpeg、内嵌 WebView2 和更新下载缓存
- 清理前显示预计释放空间并弹出原生确认
- 只删除旧运行时和更新临时文件；当前运行时、音乐、输出和历史不会被删除

**桌面界面与无障碍**

- EXE 与桌面顶栏统一使用新版品牌图标
- 主队列与设置双栏布局，视觉语言与 v0.5.1 网页版对齐（品牌渐变、图标系统、渐变进度条、结果统计卡片）
- 设置面板吸顶，开始转换按钮固定可见，无需滚动
- 高级工具默认折叠，保持主要流程简洁
- 跟随 Windows 系统深色/浅色主题，顶栏可手动切换并记住选择
- 完善键盘焦点、状态播报、错误提示和原生确认窗口
- 单实例运行，重复启动会激活已有窗口
- 支持较窄窗口的响应式单栏布局
- FFmpeg 在后台无窗口运行，批量转换不再为每个并发任务弹出终端窗口

### 支持格式

| 输入格式 | 输出格式 |
|---|---|
| `.kgg` `.kgm` `.kgma` `.vpr` `.ncm` `.kwm` | MP3、FLAC、WAV、保持原格式 |
| `.qmc0` `.qmc2` `.qmc3` `.qmc4` `.qmc6` `.qmc8` `.qmcflac` `.qmcogg` `.tkm` | MP3、FLAC、WAV、保持原格式 |

### KGG 使用说明

KGG 文件需要酷狗客户端的 `KGMusicV3.db`。程序会优先自动检测，也可以在转换设置中手动选择或重新检测。

常见路径：

```text
%APPDATA%\KuGou8\KGMusicV3.db
```

如果新下载的 KGG 无法解密，请先在酷狗客户端播放一次歌曲，再重新检测最新数据库。

### 隐私与网络

- 音频解密、转码、扫描、历史和日志全部在本机处理
- 音频文件不会发送到互联网
- 程序不会启动本地 HTTP 服务或监听转换端口
- 检查更新会访问 GitHub Release API 或预设镜像；只有用户确认自动更新后，程序才会从官方 GitHub Release 下载安装器和 SHA-256

### 软件签名与完整性

- v0.6.0 按项目决策保持未签名，Windows 可能显示“未知发布者”或 SmartScreen 提示
- 两个便携 EXE 和两个按用户安装器都必须由仓库的 v0.6.0 发布工作流构建并通过 PE 元数据与载荷检查
- Release 为每个 EXE 和安装器分别提供 SHA-256 文件，请仅从本仓库 Releases 下载并核对
- SignPath Foundation 申请和 Authenticode 签名延后到 v0.6.1 评估

### 从 v0.5.1 升级

v0.6.0 可以直接运行，不需要覆盖旧版目录。旧版浏览器历史和界面偏好不会自动迁移到桌面版，但已经转换的音频文件不会受影响。

如需暂时回退，可继续使用完整的 v0.5.1 解压目录。

### 发布前验证

<details>
<summary>展开查看完整验证记录</summary>

- 8 个 KGG 与参考实现整文件逐字节一致，覆盖 3 个 MAP、5 个 RC4
- 8 KGG、1 KGM、16 KGMA、9 NCM 共 34 个真实文件通过桌面转换核心
- KGG、KGM、KGMA、NCM 各取一个真实文件强制转为 WAV，4/4 成功并通过输出格式校验
- VPR、KWM、传统 QMC 与 KGG V2 ekey 真实样本验证完成
- Windows 10 / 11 最终验收完成，标准版与内置 WebView2 版均通过启动、转换、安装和卸载检查

</details>

### 开源许可

本项目基于 GNU General Public License v3.0 发布。内嵌 FFmpeg 与可选 WebView2 Fixed Runtime 的许可证、版本、哈希和官方来源见 `THIRD-PARTY-NOTICES.md`、`FFMPEG-SOURCE.md`、`WEBVIEW2-SOURCE.md` 与 `LICENSES/`。

---

遇到问题？[提交 Issue](https://github.com/skxxxkx666/Kugo-Music-Converter/issues)
